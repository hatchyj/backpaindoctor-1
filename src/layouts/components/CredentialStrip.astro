---
import config from "@/config/config.json";

const cred = (config as any).credentials;
const enable = cred?.enable;
const intro = cred?.intro;
const items = cred?.items ?? [];
const secondary = cred?.secondary_cta;
---

{
  enable && (
    <section class="py-8" aria-label="Practitioner credentials">
      <div class="container">
        <div
          class="credential-panel rounded-lg border border-primary/15 bg-theme-light/60 px-6 py-6 md:px-8"
          data-aos="fade-up"
        >
          {intro && (
            <p class="mb-4 text-center text-[0.8rem] font-medium uppercase tracking-wide text-text md:text-left">
              {intro}
            </p>
          )}
          <div class="row items-center justify-between gap-y-5">
            <div class="lg:col-9">
              <ul class="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:flex lg:flex-wrap lg:justify-start lg:gap-x-6">
                {items.map((item: any) => (
                  <li class="flex flex-col rounded-md border border-primary/10 bg-white/70 px-4 py-3 text-center shadow-sm lg:border-0 lg:bg-transparent lg:px-0 lg:py-0 lg:text-left lg:shadow-none">
                    <span class="text-[0.95rem] font-semibold text-dark">
                      {item.label}
                    </span>
                    <span class="text-[0.78rem] text-text">{item.detail}</span>
                  </li>
                ))}
              </ul>
            </div>
            {secondary?.label && (
              <div class="lg:col-3 text-center lg:text-right">
                <a
                  href={secondary.link}
                  class="btn btn-outline-primary text-[0.85rem]"
                >
                  {secondary.label}
                </a>
              </div>
            )}
          </div>
        </div>
      </div>
    </section>
  )
}
