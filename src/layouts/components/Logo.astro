---
import config from "@/config/config.json";
import { Image } from "astro:assets";
const { src } = Astro.props;
const {
  logo,
  logo_width,
  logo_height,
  logo_text,
  title,
}: {
  logo: string;
  logo_width: any;
  logo_height: any;
  logo_text: string;
  title: string;
} = config.site;
---

<a href="/" class="navbar-brand block">
  <span class="mobile-wordmark md:hidden">{logo_text}</span>
  {
    src || logo ? (
      <Image
        width={logo_width.replace("px", "") * 2}
        height={logo_height.replace("px", "") * 2}
        src={src ? src : logo}
        alt={title}
        class="hidden h-7 w-auto md:block md:h-[36px] md:w-[177px]"
      />
    ) : logo_text ? (
      logo_text
    ) : (
      title
    )
  }
</a>
